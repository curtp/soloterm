services:
  soloterm:
    build: .
    container_name: soloterm
    ports:
      - "8080:8080"
    volumes:
      # Mount your local project directory to /app in the container
      - ./:/app
    stdin_open: true
    tty: true
    working_dir: /app
